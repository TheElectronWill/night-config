--- core/src/main/java/com/electronwill/nightconfig/core/InMemoryFormat.java	2018-11-18 13:39:22.762335000 +0100
+++ android/core_android/src/main/java/com/electronwill/nightconfig/core/InMemoryFormat.java	2018-11-18 13:39:22.742335000 +0100
@@ -14,16 +14,16 @@
 public final class InMemoryFormat implements ConfigFormat<Config> {
 	static final Predicate<Class<?>> DEFAULT_PREDICATE =
 		type -> type == null
-				|| type.isPrimitive()
-				|| type == Integer.class
-				|| type == Long.class
-				|| type == Float.class
-				|| type == Double.class
-				|| type == Boolean.class
-				|| type == String.class
-				|| type == NullObject.class
-				|| Collection.class.isAssignableFrom(type)
-				|| Config.class.isAssignableFrom(type);
+			|| type.isPrimitive()
+			|| type == Integer.class
+			|| type == Long.class
+			|| type == Float.class
+			|| type == Double.class
+			|| type == Boolean.class
+			|| type == String.class
+			|| type == NullObject.class
+			|| Collection.class.isAssignableFrom(type)
+			|| Config.class.isAssignableFrom(type);
 
 	private static final InMemoryFormat DEFAULT_INSTANCE = new InMemoryFormat(DEFAULT_PREDICATE);
 	private static final InMemoryFormat UNIVERSAL_INSTANCE = new InMemoryFormat(t -> true);
